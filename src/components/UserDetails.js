import React, { PropTypes } from 'react'
import { Link } from 'react-router'

const UserDetails = (props) => {
  return (
    <div>
      <Link to="/account/createMenu">Create Menu</Link>
    </div>
  )
}

export default UserDetails
